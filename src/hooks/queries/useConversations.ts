// Deprecated placeholder. The old schema (title/category/scenario/goals)
// was never wired to a real table. The active conversation hooks now live
// in `useConversationResults.ts` and operate on the `conversations` +
// `conversation_analyses` tables.
export {}
